<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gratiasdeveloper.horee">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".GaleryVideo"></activity>
        <activity android:name=".hal7" />
        <activity android:name=".Video.ViewGambar"/>
        <activity android:name=".hal6" />
        <activity android:name=".hal5" />
        <activity android:name=".hal4" />
        <activity android:name=".hal3" />
        <activity android:name=".hal2" />
        <activity android:name=".hal1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>